server:
  port: 8080
spring:
  application:
    name: DmartService
  datasource:
    url: jdbc:mysql://localhost:3306/Genpact_Dmart
    username: root
    password: Rutuja@1998
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
    